spring:
  # JDBC ??
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driverClassName: org.h2.Driver
  # H2 Database ??
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
